{
  "name": "codex-plugin-zcode",
  "version": "1.0.6-zcode.1",
  "private": true,
  "type": "module",
  "description": "Use Codex from ZCode to review code or delegate tasks. ZCode port of openai/codex-plugin-cc.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "prebuild": "mkdir -p plugins/codex/.generated/app-server-types && codex app-server generate-ts --out plugins/codex/.generated/app-server-types",
    "build": "tsc -p tsconfig.app-server.json",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  }
}
